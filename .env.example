# FPL MCP Server Configuration
# ============================
# Copy this file to .env and configure as needed

# =============================================================================
# MODE: Set to "local" for docker-compose managed databases, "external" for
# cloud-hosted PostgreSQL/Redis (Azure, AWS, etc.)
# =============================================================================
MODE=local

# =============================================================================
# LOCAL MODE SETTINGS (used when MODE=local)
# Docker-compose will spin up PostgreSQL and Redis containers
# =============================================================================
POSTGRES_PASSWORD=fplpass

# =============================================================================
# EXTERNAL MODE SETTINGS (used when MODE=external)
# Configure these to connect to external PostgreSQL and Redis instances
# =============================================================================

# PostgreSQL Connection
# Format: postgresql://user:password@host:port/database
# Examples:
#   Azure: postgresql://user:pass@myserver.postgres.database.azure.com:5432/fpl?sslmode=require
#   AWS RDS: postgresql://user:pass@mydb.xxx.us-east-1.rds.amazonaws.com:5432/fpl
#   Supabase: postgresql://postgres:pass@db.xxx.supabase.co:5432/postgres
DATABASE_URL=

# Redis Connection
# For Azure Cache for Redis, AWS ElastiCache, or other hosted Redis
REDIS_HOST=
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_SSL=false

# =============================================================================
# AUTHENTICATION (optional, works in both modes)
# =============================================================================
# Bearer token for API authentication (leave empty to disable)
API_KEY=

# =============================================================================
# OPTIONAL OVERRIDES
# =============================================================================
# MCP Server port (default: 8000)
# PORT=8000

# Log level (default: INFO)
# LOG_LEVEL=INFO

# FPL API User Agent
# FPL_USER_AGENT=fpl-mcp/1.0
